.entry          # program entry point
	psh: 5      # push 5 to the stack
	psh: 2      # push 2 to the stack
	add         # add them together
	por: &ex    # move the result into the exitcode register

	hlt         # halt the program
