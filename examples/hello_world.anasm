mac STDOUT   = 1
let MSG char = "Hello, world!\n"

.entry                 # Program entry point
	psh MSG            # Text to output
	psh (sizeof MSG)   # MSG length
	psh STDOUT         # Write to file stdout
	wrf                # Write
